# No environment variables are required yet.
#
# Notes are held in memory (see src/lib/noteStore.ts) until MongoDB is wired up.
# When it is, uncomment this and point it at a local instance or your own Atlas
# cluster:
#
# MONGODB_URI=mongodb://localhost:27017/pink-stem
